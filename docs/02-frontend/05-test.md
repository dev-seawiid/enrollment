# 테스트 계획

checklist 기반 핵심 테스트. "이게 깨지면 서비스가 안 된다"를 기준으로 선별.

## 도구

| 유형      | 도구                                                        |
| --------- | ----------------------------------------------------------- |
| Unit      | vitest                                                      |
| Component | vitest + @testing-library/react + @testing-library/jest-dom |
| E2E       | Playwright                                                  |

---

## Unit Tests

### 스키마 검증

| 테스트                                                                 | 상태    |
| ---------------------------------------------------------------------- | ------- |
| `signupSchema` — 비밀번호 복합 규칙, 전화번호 변환, 비밀번호 확인 일치 | ✅ 27개 |
| `loginSchema` — 이메일 형식, 비밀번호 필수                             | ✅ 3개  |
| `courseSchema` — 강의명 필수, 수강 인원 ≥ 1, 가격 ≥ 0, 소수점 차단     | ✅ 6개  |

### API 응답 런타임 검증

| 테스트                                                     | 상태   |
| ---------------------------------------------------------- | ------ |
| `coursePageSchema` — 정상/빈 배열 parse, 필드 누락 시 실패 | ✅ 4개 |

---

## Component Tests

### RBAC — 강의 개설 버튼 노출 (checklist: 권한 기반 UI/UX 제어)

| 테스트                                              | 상태 |
| --------------------------------------------------- | ---- |
| 강사 로그인 시 "강의 개설" 버튼 노출                | ✅   |
| 수강생 로그인 시 "강의 개설" 버튼 비노출            | ✅   |
| 비로그인 시 "로그인" 버튼 노출 + "강의 개설" 비노출 | ✅   |

### 정렬 탭 API 요청 (checklist: 정렬 로직)

| 테스트                                                 | 상태 |
| ------------------------------------------------------ | ---- |
| "신청자 많은 순" 클릭 → `onSortChange("popular")` 호출 | ✅   |
| "신청률 높은 순" 클릭 → `onSortChange("rate")` 호출    | ✅   |

### 강의 카드 인터랙션 (checklist: 다중 신청)

| 테스트                                                        | 상태 |
| ------------------------------------------------------------- | ---- |
| 마감 강좌 — "마감" 뱃지 표시 + 클릭 시 onToggle 미호출        | ✅   |
| 일반 강좌 클릭 → onToggle 호출                                | ✅   |
| info 버튼 클릭 → onDetail 호출, onToggle 미호출 (이벤트 분리) | ✅   |

### 비밀번호 입력

| 테스트                                           | 상태   |
| ------------------------------------------------ | ------ |
| 초기 type=password, 토글 시 text ↔ password 전환 | ✅ 3개 |

---

## E2E Tests (Playwright)

### 라우트 보호 (checklist: 비로그인 보호 + RBAC)

| 테스트                                                        | 상태      |
| ------------------------------------------------------------- | --------- |
| 비로그인 → `/courses` → `/login` 리다이렉트                   | ❌ 미작성 |
| 수강생 → `/courses/new` URL 직접 접근 → `/courses` 리다이렉트 | ❌ 미작성 |
| 로그인 상태 → `/login` 접근 → `/courses` 리다이렉트           | ❌ 미작성 |

### 핵심 흐름

| 테스트                                        | 상태      |
| --------------------------------------------- | --------- |
| 회원가입 → 자동 로그인 → 강의 목록 리다이렉트 | ❌ 미작성 |
| 다중 선택 → 수강 신청 → 결과 다이얼로그       | ❌ 미작성 |

---

## 요약

| 유형      | 케이스 | 작성됨 |
| --------- | ------ | ------ |
| Unit      | 40     | ✅ 40  |
| Component | 10     | ✅ 10  |
| E2E       | 5      | ❌ 0   |
| **합계**  | **55** | **50** |
